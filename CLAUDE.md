# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Рабочий язык репозитория — **русский**: все документы, комментарии и коммит-сообщения пишутся по-русски.

## Что это за репозиторий

Подготовка команды к соревнованию **«Компьютерное зрение в навигации беспилотных роботизированных систем»** (Архипелаг, Москва, 2026). Два этапа: заочный (алгоритм маршрута по картинке карты, платформа `sim.avt.global/public/183`) и очный (три дня на площадке: беспилотный автомобиль АЙКАР + квадрокоптер Geoscan Pioneer Mini 2 + визуализатор, 100 баллов суммарно).

Написаны **пункты 1 и 2 плана**:

- **2.3.1** (взлёт по команде, облёт территории, посадка) — бортовое ПО дрона, распределительный хаб, общий протокол. Написан, **полётом не проверен** (дрона нет рядом);
- **2.3.2** (обнаружение электростанций) — калибровка камеры, локализация «пиксель → метры», детектор, реестр станций, визуализатор карты. Проверен **без дрона**: заглушка камеры рисует станции той же дырочной моделью, которой локализация считает координаты, поэтому цепочка «мок → детектор → координаты → реестр → пакет → визуализатор» прогоняется целиком, а «<30 см» проверяется тестом.

Подзадачи запускаются **по отдельности** ключом `--task`: 2.3.1 идёт вообще без детекции и потому не может сломаться ложным срабатыванием. Пункты 3–4 (статусы, удаление пыли) — точки расширения в уже готовом автомате, см. [docs/DRONE_PLAN.md](docs/DRONE_PLAN.md).

## Структура

| Путь | Роль |
| --- | --- |
| `квадрокоптер/` | Бортовое ПО: `main.py` (точка входа), `mission.py` (автомат), `tasks.py` (профили подзадач), `flight.py` (обёртка SDK и оси), `survey.py` (геометрия маршрута), `localization.py` (пиксель → метры), `fusion.py` (реестр станций), `perception/` (калибровка, детектор, конвейер), `config.py` + `config.yaml`, `mock_pioneer.py` (заглушка дрона) |
| `распределительный хаб/` | `protocol/` — формат пакетов, HTTP-транспорт и привязка карты, **единый источник правды**; `hub.py` — ретранслятор (+ мост `--forward` в формат организаторов); `transmitter.py` — эмулятор автомобиля и дрона; `visualizer.py` — карта станций |
| `беспилотный автомобиль/` | Чужой трек; пока только README со входом в общий протокол |
| `launch.py` | Пульт: ssh-ключ, заливка на борт, запуск хаба, визуализатора и дрона, команда на взлёт — одной командой (см. `RUN.md` §0) |
| `tests/` | pytest, дрона и сети не требуют |
| `tools/` | `check_sdk.py`, `check_axes.py`, `diag_model.py` — диагностика борта; `record.py` — запись попыток; `replay.py` — прогон перцепции по записи; `calibrate_camera.py` — интринсики; `eval_detection.py` — mAP и confusion matrix; `check_map.py` — проверка привязки карты глазами; `handheld.py` — камера с рамками станций **без взлёта** (дрон носят на руках) и разбор готовых снимков; `imgio.py` — чтение/запись картинок по путям с кириллицей (`cv2.imread` их на Windows молча не открывает, а директории по регламенту 2.9 русские) |
| `RUN.md` | Что запускать и в каком порядке; таблица «если не работает». Первое, что читать перед полигоном |
| `docs/REGLAMENT.md` | Дословная транскрипция официального PDF. Опечатки оригинала помечены `[sic]` и сносками. **Свои выводы сюда не дописывать** |
| `docs/NOTES.md` | Разбор регламента: сумма баллов, приоритеты, ловушки, разбор задачи заочного этапа |
| `docs/DRONE_PLAN.md` | Главный проектный документ: архитектура бортового ПО дрона (подзадачи 2.3.1–2.3.4), бюджет ошибки локализации, порядок работ по дням, риски |
| `docs/pioneer_mini2.md` | Железо и SDK по сайту Geoscan |
| `docs/organizer_handouts.md` | Разбор раздаток организаторов (`HTTP_help.pdf`, описание классов SDK, `MapExample.py` — §2.3) |
| `docs/lessons_from_archipelago.md` | Разбор чужого репозитория рядом: что переносится, что нет |
| `docs/assets/` | Картинки к документам |
| `records/` | **Единственное место артефактов прогонов**: `flight_*.mp4` + `flight_*.jsonl` (`mission.record_dir`) и `hub_log.jsonl` (`hub.py --log`). Создаётся сама, в git не идёт, оттуда же `launch.py` забирает записи с борта (`workspace/records`). Имя латиницей намеренно: `cv2.VideoWriter` на Windows молча не открывает путь с кириллицей |
| `фото станции/`, `фото площадки/` | Снимки организаторов (`чистые/`, `грязные/`) и площадки. По ним настраивался детектор, см. ниже |
| `source/` | Всё присланное организаторами как есть: PDF, из которых сделаны транскрипции, и `2_step/` (`MapExample.py` + картинка карты полигона). В корне ничего из этого не держим |
| `water_object_monitoring_archipelago_2026-develop/` | **Чужой репозиторий-референс**, см. ниже |

## Соглашения документации

- Документы **связаны перекрёстными ссылками** и на конкретные параграфы (`§4.2`). Меняя факт в одном файле, проверь ссылающиеся на него места — особенно `DRONE_PLAN.md`, он опирается на все остальные.
- Каждая выжимка начинается с источника и даты сбора («Собрано: 2026-07-31») и явно отделяет пересказ источника от собственных выводов.
- `⚠️` — незакрытый вопрос или расхождение источников; такие блоки стоят прямо в тексте плана, а не отдельным списком.
- Открытые вопросы к организаторам копятся в `DRONE_PLAN.md` §11, проверки на борту в день 1 — в `organizer_handouts.md` §4.3 и `lessons_from_archipelago.md` §9.

## Соглашения кода

- **Идентификаторы**: имена модулей, классов и публичных методов — английские (их видит чужой код: автомобиль, визуализатор, SDK), внутренние переменные, вспомогательные функции и тесты — русские. Комментарии объясняют **почему**, со ссылкой на источник (`регламент 2.5`, `docs/organizer_handouts.md §3.1`), а не пересказывают код.
- **Бортовой код не тянет зависимостей**: только стандартная библиотека плюс то, что уже стоит в Pioneer OS (`numpy`, `cv2`, `pioneer_sdk2`, `pioneer_rknn`). Установка пакета на борт требует смены режима Wi-Fi и потери адреса `172.17.49.2`, поэтому `квадрокоптер/` и `protocol/` не импортируют ни `flask`, ни `requests`, ни `PyYAML` — даже там, где они на нашем борту фактически есть.
- **Совместимость с Python 3.8** (`from __future__ import annotations`, `class X(object)`, без `list[int]` и `match`): того же кода касается платформа заочного этапа, а состав выданной на площадке машины заранее неизвестен.
- Новый код **обязан проверяться без дрона**: геометрия и протокол — тестами, полётная логика — через `PIONEER_MOCK=1`.

## Три источника по железу расходятся

Сайт Geoscan, раздатка организаторов и полевой опыт чужой команды дают **разные факты об одном и том же дроне**. Приоритет — за полевым опытом, но всё перепроверяется на своём борту. Расхождения сведены в таблицы `lessons_from_archipelago.md` §1 и `organizer_handouts.md` §1. Самые опасные, не «упрощать» их в документах:

- **Оси `go_to_local_point`**: по раздатке Y — вперёд, X — вправо, ноль фиксируется в `arm()`. Ошибка не падает с исключением, а разворачивает траекторию на 90°. Решение плана — единственный адаптер СК в `flight.py`, остальной код в правой СК. ✅ **Проверено полётом 31.07.2026** (`tools/check_axes.py`): раздатка права, сайт Geoscan — нет. Значения `axes` в `config.yaml` подтверждены, менять их не нужно.
- **`CameraType.OPT` не открывать** — это камера оптического потока позиционирования, второй потребитель сажает навигацию.
- **`get_cv_frame()` отдаёт BGR**, модель обучена на RGB → `cvtColor` перед инференсом.

### Что подтверждено на нашем борту 31.07.2026 (`tools/check_sdk.py`)

Отчёт лежит на борту в `~/workspace/check_sdk_report.txt`. Значения перенесены в `config.yaml`.

| Факт | Значение | Следствие |
| --- | --- | --- |
| **`Pioneer.is_landed()` НЕ СУЩЕСТВУЕТ** | метода нет, хотя он есть в примерах | `Flight.is_landed()` идёт цепочкой `is_landed → get_fly_state → дальномер → LPS`. Прямой вызов бросал `AttributeError`, тот глушился, и дрон получал `disarm` в воздухе |
| `ServoCamera.set_angle` | строго **−80…+30**, вне — `ValueError` | надира нет, полевой опыт чужой команды подтверждён, раздатка (−90) ошибалась |
| SoC | **`rk3576`** (`geoscan,pioneermini2 rockchip,rk3576`) | `target_platform=rk3576` при конвертации `.rknn` |
| Дальномер | работает **`get_dist_sensor_data()`**; `get_ranger_data()` отдаёт пять `None` («Модуль Ranger не установлен») | `Flight.range_down()` пробует оба и запоминает работающий |
| `get_local_position_lps` | **только без аргументов**; с позиционным `True` — `TypeError` | пример организаторов в этом месте неверен |
| `get_nav_status_lps` | перечисление `NavStatus`, в покое `OK` | сравнивать по `.name`, а не по числу |
| Разрешение кадра | **1080×720**, `uint8`, BGR | нужно для пересчёта площади объекта в м² (пункт 2) |
| Порт `ImageViewer` | на **8889** что-то отвечает, 8089 закрыт | ⚠️ `check_sdk.py` только сканировал порты и **настоящий поток не проверял** — «путь `/video`» из прежней редакции этой строки был домыслом. По документации Geoscan `ImageViewer` вещает **по RTSP через GStreamer** и доступен **только при драйвере камеры gstreamer**; 8889 при таком раскладе похож на WebRTC-выход медиасервера (у mediamtx RTSP 8554 / HLS 8888 / WebRTC 8889), то есть браузер и VLC хотят разные адреса. Поток живёт, только пока процесс публикует кадры. Поэтому `tools/handheld.py` поднимает **свою** трансляцию (`tools/mjpeg.py`, stdlib + cv2) на `http://172.17.49.2:8080/` и от SDK не зависит |
| Напряжение батареи | **8.2 В** в покое → это **2S**, а не 1S из документации | пороги `batt_low`/`batt_critical` подняты до 7.0/6.6 В; со старыми 6.6/6.2 защита не сработала бы никогда |
| Python на борту | **3.12.3**, `numpy` 1.26, `cv2` 4.10, **`PyYAML` и `requests` есть**, `flask` нет | ставить на борт по-прежнему ничего не нужно |
| `pioneer_rknn` | `Yolo.arch = ['yolov8','yolov11']`, `ModelContainer.arch = ['custom']`, реестр моделей пуст | подтверждает план: raw-branch модель регистрировать как `custom` |
| Событий SDK больше, чем в выжимке | есть `LOW_CHARGE`, `LOW_VOLTAGE1/2`, `POINT_DECELERATION` | кандидаты заменить опрос батареи подпиской (пункт 4) |

### Что подтверждено полётом 31.07.2026 (`tools/check_axes.py`)

| Факт | Значение | Следствие |
| --- | --- | --- |
| **Оси СК автопилота** | первый аргумент `go_to_local_point` увёл дрон **вправо**, второй — **вперёд** | СК левая (X вправо, Y вперёд), как в раздатке организаторов. `axes: swap_xy: true, sign_x: -1, sign_y: 1` — подтверждено, `sdk_x = −наш_y`, `sdk_y = наш_x` |
| **Единицы `yaw` — по-прежнему открыты** | на `yaw=90` дрон **не раскрутился**, но и поворота на четверть оборота наблюдатель не подтвердил; `get_orientation()[2]` не изменился (0.11 → 0.09) | радианы исключены (90 рад — это ~14 оборотов), но возможно, что четвёртый аргумент просто игнорируется. Оставлено `yaw_units: deg`. **Маршрут от этого не зависит**: все точки в `survey.py` летятся с `yaw=0`, курс фиксируется в `arm()` |

### Что НЕ проверено и чем это грозит

Всё перечисленное лежит в `config.yaml` с пометкой «НЕ ИЗМЕРЕНО»/«НЕ ПРОВЕРЕНО». Ни одно
не падает с исключением — все дают правдоподобный, но неверный результат.

| Что | Чем закрывается | Цена ошибки |
| --- | --- | --- |
| **Интринсики камеры** (`camera.intrinsics`) | `tools/calibrate_camera.py`, шахматка, 15 минут, **полигон не нужен** | 4 балла за «<30 см». Пока пусты, локализация работает по грубой оценке и **громко ругается** при старте |
| **`R_mount`** — перекос крепления (`camera.mount`) | метка на известном расстоянии + `tools/replay.py`, **при рабочем угле сервы** | систематический сдвиг всех координат разом |
| **Знаки `roll`/`pitch`** от `get_orientation()` (`camera.attitude`) | наклонить дрон рукой на земле и сверить знак | удвоение вклада крена, ~7 см на градус |
| **Единицы `yaw`** | повторный полёт с наблюдателем (`tools/check_axes.py`, тест 3) | маршрут не зависит: все точки летятся с `yaw=0` |
| **Как станция выглядит на фоне покрытия** (`detector.polarity`) | вопрос организаторам + первый же снятый кадр | на карте организаторов покрытие тёмное, не светлое |
| **Разброс станций по площади** (`zone.width/height`) | самая дальняя станция в каждую сторону **плюс 0.4 м** | зона меньше разброса — потерянная станция; без прибавки крайние станции не попадают в центр кадра, и это 4 балла |

### Что подтверждено рулеткой на площадке 01.08.2026

| Факт | Значение | Следствие |
| --- | --- | --- |
| **Размер станции сверху** | **29 x 21 см** = 0.061 м² | `detector.min/max_area_m2` сужены с `0.03…1.20` до `0.025…0.15`. Прежняя верхняя граница была в 20 раз больше станции — в неё пролезали тень, разметка и посадочный знак. Вилка 0.4x–2.5x держит ошибку оценки площади из некалиброванного `fov_h_deg`; **после калибровки сузить до 0.04…0.10** |
| **Расстояние между станциями** | не меньше **25 см между краями** | между центрами — не меньше 25 + 21 = **46 см**, и реестр сравнивает именно центры. `fusion.assoc_radius` снижен с 0.40 до **0.20**: со старым порогом две станции молча становились одной |
| **Станции разбросаны по площади** | не вдоль трассы | `route.mode` переключён `loop → survey`. Контур овала их бы не накрыл |
| **Площадка сменилась** (01.08.2026, вторая) | прямоугольник **4.6 м вперёд × 6.7 м влево**, дрон в ближнем **правом** углу | `zone.width/height` = 4.6/6.7, `center_x: 2.3`, `center_y: 3.35`, `strip_axis: x` (иначе `auto` повёл бы проходы вдоль длинной стороны). Змейка: **7 полос, 31.6 м, ~82 с хода** — прежние попытки были втрое короче, батарею смотреть отдельно. Секция `loop` (2.7×1.7) осталась от первой площадки и к этой не относится |
| **Маршрут задан явно** (01.08.2026, третья редакция) | **4.4 м вперёд, 1.5 м влево, 4.4 назад, 1.5 вправо**, дрон в углу | добавлен режим `route.mode: rect` и секция `rect` (`length: 4.4`, `width: 1.5`, `side: left`); умолчание переключено `survey → rect`. 11.8 м, ~36 с хода, старт и финиш на «H». ⚠️ Проходы идут по краям фигуры: середина (y≈0.75 м) попадает только на **край** кадра, значит станция там найдётся, но координата будет грубее — `plan_rect` об этом предупреждает при старте, это не сбой. Секции `zone` (змейка) и `loop` (контур) остались от прежних расстановок |
| **Привязка карты** (`map.*`) | 74 пикселя = 0.5 м → `px_per_m: 148`; дрон в пикселе **(998, 701)**; нос вверх по картинке → `yaw_deg: 0` | вся картинка `assets/map.jpg` (1204×891) выходит 8.14 × 6.02 м, зона ложится в (6…998, 20…701) — сходимость и есть проверка. `yaw_deg` отсчитывается от «вверх по картинке» против часовой: влево +90, вниз 180, вправо −90. Проверяется глазами: `tools/check_map.py` |

### Как станция выглядит на самом деле (`фото станции/`, разобрано 01.08.2026)

Фотографии организаторов с площадки, папки `чистые/` и `грязные/`. Опровергают два допущения, на которых был построен план:

- **Пол тёмно-серый со светлой разметкой**, по краям площадки светлый ковролин. Панель тоже тёмная — тёмные ячейки на тёмном полу, — но с **радужной голографической рамкой и сеткой между ячейками**, которые заметно светлее фона. Станция отклоняется от фона в **обе стороны сразу**, поэтому `detector.polarity: any` — не «пока неизвестно», а единственный работающий вариант.
- 🔴 **«Покрыта пылью» — это НЕ равномерный налёт и НЕ повышенная яркость.** Пыль изображена **цветными печатными многогранниками, положенными на панель** (красные, жёлтые, белые, синие, по 1–3 штуки). Признак стал **цветовым**: в аугментациях выключать `hsv_h`, а не `hsv_v`, и таблица признаков в [DRONE_PLAN.md](docs/DRONE_PLAN.md) §3.2 переписана — три признака из четырёх работали в обратную сторону. Ловушка: радужная рамка тоже насыщенно-цветная и есть у **любой** станции, поэтому цветные пятна искать только **внутри** маски, с отступом от границы.
- Рядом со станциями на полу лежат **отдельные серые комки и синие предметы** (конус, щётка). Они станцией не являются, а по размеру близки к нижнему порогу площади — главные кандидаты в ложные срабатывания вместе с белым логотипом и надписями на покрытии.
- Для подзадачи 2.3.2 всё это неважно: **и чистые, и грязные считаются станциями**, классический детектор классов не различает. Важно для 2.3.3.

#### 🔴 Классический детектор был слеп, и заглушка это скрывала

Прогон фотографий через боевой конвейер (`tools/handheld.py --image`) 01.08.2026 показал: робастный разброс фона на настоящих кадрах **24–85 уровней яркости**, а не почти ноль, как у заглушки. Порог `contrast_sigma × разброс` при прежнем `contrast_sigma: 6.0` выходил **142–507 при физическом максимуме отклонения 255** — на четырёх снимках из девяти детектор не мог сработать в принципе, на остальных пяти давал ноль кандидатов. Ошибок в лог при этом не пишется: просто ничего не находится.

Заглушка показать это не могла **по устройству**: она рисует станции на однородном фоне, где разброс близок к нулю и порог всегда падает на нижнюю границу `contrast_min`. Это тот же класс отказа, о котором предупреждает раздел про RKNN («ноль детекций без единой ошибки»), только в классическом пути.

Сделано: `contrast_sigma` снижен до **2.0** (провизорно, подтвердить кадром с борта) и добавлен **потолок `contrast_max: 0.5`** — порог не может превысить половину диапазона, а когда упирается, детектор кричит в лог. Лишние кандидаты ловятся площадью, вытянутостью и счётом станций; молчаливый ноль не ловится ничем.

Что ещё видно на размеченных снимках и **не закрыто**:

- **панель не собирается в одно пятно.** Её тёмные ячейки почти не отличаются от тёмного пола, в маску попадают только светлые линии голографической сетки — вместо одного прямоугольника выходят десятки обрывков. Это свойство контраста, а не оптики, значит повторится и на камере дрона. Перебор `close_px` (9→81) по фотографиям устойчивого значения не дал;
- **белый логотип «ТЕХНОБОТ» проходит как станция** — подтверждённое ложное срабатывание, а не гипотеза;
- **серые комки мусора дают 0.023 м²** при нижнем пороге 0.025 — впритык;
- разметка и пунктир отсеиваются по `aspect_max` штатно.

Дальше настраивать по телефонным снимкам **нельзя** (другая оптика, у каждого кадра свой масштаб). Только `tools/handheld.py` на борту, над настоящей станцией.

## Архитектура кода

Три программы, общающиеся **только через хаб** по HTTP на порту 5001: дрон (`квадрокоптер/main.py`, на борту), хаб (`распределительный хаб/hub.py`, на компьютере оператора либо на борту) и передатчик-эмулятор (`transmitter.py`, играет за автомобиль или за дрона). Транспорт выбран HTTP, а не UDP-broadcast из первой редакции плана: раздатка организаторов показывает Flask + `requests` как ожидаемый способ связи и объясняет смысл «распределительного хаба» как ретранслятора одному-многим.

- **`распределительный хаб/protocol/` — единый источник правды** для дрона, передатчика, автомобиля и визуализатора. Никто не собирает JSON руками, включая `launch.py`. В финале дрон подставляется вместо передатчика, и код автомобиля не должен заметить разницы — это главное архитектурное требование задания.
- **`visualizer.py` — карта станций, и без неё 8 баллов подзадачи 2.3.2 не даются.** Сделан по присланному организаторами `MapExample.py`, но с двумя отличиями: **не поднимает своего сервера** (пример слушает порт 5001 — тот же, что наш хаб; здесь визуализатор работает клиентом хаба) и **ведёт станции по `id`**, а не по счётчику пришедших сообщений, как пример. Координаты приходят в метрах, перевод в пиксели карты — `protocol/legacy.py → MapAnchor` по секции `map` конфига.
- **`protocol/legacy.py` + `hub.py --forward` — мост наружу** в текстовый формат организаторов (`POST /fly` с телом `"x,y"` в пикселях, `POST /eyecar` с `Good|Broken`). Нужен на один случай: если визуализатор на площадке будет писаться смежным треком по шаблону организаторов. По умолчанию выключен — хаб обязан оставаться тупым ретранслятором. Наш JSON-протокол при этом остаётся: текстовый формат не везёт ни `id`, ни трёх статусов, ни номера пакета для отсева повторов.
- **`messages.py`** держит контракт: 5 типов пакетов, `SOURCES`, `STATUSES`, `MISSION_STATES`, валидация. **Строки состояний дублируются** в `mission.py` и `MISSION_STATES` — расхождение означает молча отвергнутый пакет телеметрии, менять только вместе.
- **`transport.py` (`HttpTransport`)** — stdlib, без `requests`: `send()` не блокирует (отправка в своём потоке, поток перцепции не ждёт сеть); телеметрия хранится в очереди в единственном последнем экземпляре, а события (`station_new`) — в отдельной непотеряемой очереди; недоступный хаб **не роняет полёт**, ошибки только считаются.
- **Два потока на борту, и это принципиально.** Главный — перцепция (кадр → инференс → координаты → реестр → HUD → трансляция → запись), отдельный — полётная логика: `Pioneer(wait_callback=True)` делает команды блокирующими, и миссия из главного потока останавливала бы инференс на каждом перелёте. Обмен — только через `MissionState` под замком. К SDK обращается один поток (полётный) плюс камера в главном.
- **`mission.py`** — автомат `IDLE → ARM → TAKEOFF → SURVEY → (DUST) → RETURN → LAND → DISARM`. Аварийные переходы из любого состояния сделаны исключением `AbortMission`, которое всплывает даже изнутри блокирующего перелёта. `DUST` — заготовленная точка расширения для пункта 4. Телеметрия и проверка аварий живут в `_tick()`, который `Flight` дёргает во время перелёта, — поэтому третьего потока не нужно.
- **`tasks.py` — профили подзадач**, единственное место, где записано, что включено в какой попытке. `2.3.1` не создаёт ни детектора, ни реестра вообще: пункт 1 физически не может сломаться ложной детекцией. Выбор — `config.yaml → mission.task` либо ключ `--task` у `main.py` и `launch.py`. Профили лежат в коде намеренно: это структура решения, а не настройка под расстановку, и самодельный YAML-парсер не умеет ключи вида `"2.3.1"`.
- **`localization.py` — вся геометрия «пиксель → метры»**, без SDK, cv2 и numpy: матрицы 3×3 на кортежах. Прямая и обратная формулы (`pixel_to_ground`, `ground_to_pixel`) строго обратны друг другу — на этом держится сквозной тест, и этой же обратной формулой заглушка камеры рисует станции.
- **`perception/calib.py`** — интринсики, дисторсия и `R_mount(угол сервы, перекос)`. Пока `camera.intrinsics` в конфиге пусты, работает грубая оценка по `zone.fov_h_deg`, и об этом **громко предупреждается** при каждом старте: иначе «координаты уехали» будут искать в детекторе.
- **`perception/detector.py`** — классический CV (отклонение от фона, площадь в м²) и YOLO-RKNN raw-branch с нашим декодером. `create_detector` при неудаче с YOLO падает на классический и говорит об этом. **Морфология разнесена на две операции**: открытие (`close_px`, пиксели) чистит шум и обязано быть мелким, иначе съедает тонкие линии сетки панели; закрытие (`close_m`, **метры**) сшивает обрывки панели в одно пятно и пересчитывается в пиксели по текущей высоте — размер дыры между обрывками задан геометрией панели, а не картинкой, ровно как и порог площади.
- **`fusion.py` — реестр станций**, только stdlib. Ассоциация по мировым координатам (20 см), подтверждение с 3 наблюдений, координата — **медиана**. **Защита от раскола**: между `assoc_radius` и `min_separation` лежит мёртвая зона — наблюдение слишком далеко, чтобы уточнять координату, но ближе, чем вторая станция может стоять физически (замер: 25 см между краями + 21 см станции = 46 см между центрами). Такое наблюдение засчитывается ближайшей станции **грубым**, а не заводит новую запись: одна станция, пришедшая двумя, стоит те же 8 баллов, что и пропущенная. Счётчик `prevented_splits` печатается в итоге — большое число значит, что лечить надо причину (`detector.close_m`), а не симптом. Наблюдения делятся на **точные** (центр кадра) и **грубые** (края): грубые подтверждают станцию наравне, но координату дают только точные — иначе станция, прошедшая по краю кадра, потерялась бы совсем. `station_new` дошлётся с тем же `id`, когда медиана уточнится, и ещё раз общим `flush()` перед `recon_done`.
- **`flight.py` — единственное место, где живут оси автопилота.** Наружу выставляется наша правая СК (X вперёд, Y влево, Z вверх), пересчёт делает `AxisAdapter` по `config.yaml → axes`. Там же нормализация `yaw` (наружу всегда градусы), проверка `h_min` и обход расхождений в именах методов SDK (`range_down`, позиция, `is_landed`) — «пробуем оба варианта, запоминаем работающий».
- **`survey.py` — чистая геометрия**, без SDK и камеры: режимы `rect` (прямоугольник от точки взлёта, основной), `loop` (облёт контура трассы вокруг площадки) и `survey` (змейка по прямоугольнику) возвращают один и тот же `Route`, автомат их не различает. Поэтому маршрут полностью покрывается тестами без дрона. У `rect` точка взлёта стоит в списке дважды — первой и последней: первый проход вперёд иначе не попадал бы в `plan.length`, и оценка расхода батареи занижалась бы на треть периметра.
- **`config.py` читает YAML сам** (узкое подмножество: вложенные словари, скаляры, комментарии), если `PyYAML` нет. Списки, якоря и многострочные блоки в `config.yaml` не использовать — парсер их не поддерживает намеренно.
- **Раскладка путей двойная**: в репозитории модули лежат в директориях с русскими именами (регламент 2.9), на борту — плоско (`protocol/` и `perception/` рядом с `main.py`). `main.py` добавляет в `sys.path` оба варианта; `tests/conftest.py` делает то же самое.
- **Заглушка камеры рисует станции честной дырочной моделью** — той же, что в `localization.py`, только обратной, и с интринсиками из конфига. Поэтому «мок → детектор → координаты → реестр → пакет» прогоняется целиком без дрона, а «<30 см» проверяется тестом. **Чего это не ловит**: неверные интринсики и `R_mount` — заглушка рисует теми же числами, которыми потом считает, поэтому любая калибровка выглядит идеальной. Настоящая проверка — только рулетка под дроном.
- **Все константы — в `config.yaml`** (интринсики, `R_mount`, угол сервы, высоты, пороги, размеры зоны, привязка карты). Требование универсальности 2.5: захардкоженные координаты, тайминги вместо детекции и подогнанные под расстановку пороги — формальное основание не засчитать попытку. Пороги выводятся скриптами по датасету, и это же ответ эксперту на защите решения (иначе баллы аннулируются).
- **Оффлайн-разработка обязательна, а не удобна**: лимит времени по регламенту 1.3 утекает, даже когда полигон простаивает. Отсюда `mock_pioneer.py` (полная заглушка SDK, умеет ломаться по `PIONEER_MOCK_*`), `tools/record.py`, отладка по записям; полигон — только для попыток.
- **Три директории с именами из регламента 2.9** (`беспилотный автомобиль`, `распределительный хаб`, `квадрокоптер`) — под требование ежедневной сдачи архива; репозиторий уже разложен так, архив собирается из корня как есть.

## Путь RKNN (короткий путь не работает)

Проверено чужой командой на этом же железе: стандартный ONNX-экспорт с плоской головой `1×6×8400` после INT8 даёт на борту **ноль детекций без единой ошибки**. Рабочая последовательность (`lessons_from_archipelago.md` §2):

1. ONNX из форка `airockchip/ultralytics_yolo11` — сырые тензоры до decode (9 веток);
2. `rknn-toolkit2` (только x86-64 Linux → Docker), target-чип подтвердить на борту;
3. регистрировать модель с архитектурой **`custom`**, иначе `ModelContainer` постобработает сам;
4. decode (DFL-softmax + грид + NMS) — самим на CPU, ветки опознавать **по числу каналов**, RKNN их переставляет.

`.rknn` и код декодера — **пара**: raw-branch модель с плоским декодером даёт мусор без ошибок, версионировать и заливать вместе.

Датасет: сплит train/val **по клипам, а не по кадрам** (соседние кадры протекают в val и mAP врёт). Аугментации под съёмку сверху (`degrees=180`, `flipud`, `scale`); **`hsv_h` выключить** — «покрыта пылью» изображается цветными телами на панели (см. ниже), то есть признак цветовой, и jitter по оттенку травит именно его. Яркость (`hsv_v`) крутить можно и нужно.

## Референсный репозиторий рядом

`water_object_monitoring_archipelago_2026-develop/` — **код другой команды с другого соревнования** («Интеллектуальный мониторинг водных объектов», кораблики и ArUco). Ценен тем, что железо и среда те же: реально летавший код, обученная `.rknn`, скрипт заливки на борт.

- Его `CLAUDE.md`, `pipeline.md`, `регламент.md` описывают **чужую задачу** — не принимать их «Цель/Задача/Приоритеты» за наши.
- Что оттуда берётся и что нет — таблицы в `docs/lessons_from_archipelago.md` §6–7. Кратко: переносятся `vision/detector.py` (`decode_rawbranch`, `_nms`), `rknn_convert/`, `drone/diag_model.py`, `training/build_dataset.py` + `train.py`, `code/run_on_video.py`, процедура заливки из `push_to_drone.sh` (уже перенесена в `launch.py`); не переносятся `pool_roi.py`, `color.py`, `aruco_id.py`, пиксельный трекер (у нас ассоциация по мировым координатам) и их точки маршрута.
- Его собственный `.rknn`-путь описан в `rknn_convert/README.md` под `rk3566`/плоскую голову — это **устаревшая версия** их же решения, актуальная — `stable_v1.2.1` + `rk3576` + raw-branch.
- Папки `stable_v1.0…v1.2.1` — самодостаточные снапшоты рабочих версий (откат на площадке за секунды). Практика, которую план предлагает перенять под требование ежедневной сдачи. Веса в них дублировались байт в байт; дубликаты удалены 31.07.2026, `.rknn` остались по одному на пару — fp16 в `stable_v1.1/`, актуальная int8raw в `stable_v1.2.1/` (`lessons_from_archipelago.md` §5).

## Команды

Сборки нет, зависимости — `numpy`, `opencv-python`, `pytest` (`requirements.txt`; `flask` и `PyYAML` необязательны). Подробная инструкция — `RUN.md`, здесь только сами команды. `python launch.py --setup` проверяет окружение, доставляет недостающее и прогоняет тесты — это первое, что делается на выданной машине (регламент 1.4: свои ноутбуки нельзя).

```bash
# ТЕСТЫ (дрона и сети не требуют, ~75 с).
# Прогонять на НЕЗАНЯТОЙ машине: заглушка дрона работает в реальном времени,
# и под параллельной нагрузкой тесты таймаутов начинают ложно падать.
# На 2026-08-01 — 295 passed.
python -m pytest tests -q
python -m pytest tests/test_mission.py -q                 # один файл
python -m pytest tests/test_survey.py -k змейка -q        # один тест по имени

# ПРОГОН БЕЗ ДРОНА — одной командой (пульт поднимает хаб, карту и дрона сам)
python launch.py --mock --takeoff                       # подзадача из config.yaml
python launch.py --mock --task 2.3.1 --takeoff          # только облёт, без детекции
python launch.py --mock --task 2.3.2 --takeoff --viz    # разведка + карта станций

# то же вручную, четыре окна (источник правды, если пульт повёл себя странно)
python "распределительный хаб/hub.py"
python "распределительный хаб/visualizer.py" --hub http://127.0.0.1:5001
PIONEER_MOCK=1 python квадрокоптер/main.py --task 2.3.2 --hub http://127.0.0.1:5001
python "распределительный хаб/transmitter.py" --hub http://127.0.0.1:5001 takeoff

# самый быстрый прогон логики миссии, без хаба и камеры
PIONEER_MOCK=1 python квадрокоптер/main.py --manual --no-camera --no-hub

# ВИЗУАЛИЗАТОР БЕЗ ДРОНА ВООБЩЕ (проверить карту, пока борта нет)
python "распределительный хаб/transmitter.py" --hub http://127.0.0.1:5001 recon --stations 4

# БОРТ
python launch.py            # ключ -> заливка -> хаб -> дрон; t — взлёт, s — состояние, q — выход
python launch.py --push     # только залить
python launch.py --setup    # развернуть выданную машину: пакеты + тесты

# РАЗБОР ПОПЫТКИ
python tools/record.py records/flight_20260731_142530.jsonl   # что происходило
python tools/replay.py records/flight_20260731_142530.jsonl   # что бы дрон отправил
python tools/replay.py records/flight_*.jsonl --set detector.min_area_m2=0.05 --out разбор/

# КАЛИБРОВКА И ДИАГНОСТИКА (без калибровки «<30 см» не проверить)
python tools/calibrate_camera.py --grab кадры/          # снять шахматку с борта
python tools/calibrate_camera.py кадры/                 # -> готовый блок для config.yaml
python tools/eval_detection.py кадры/ разметка/         # mAP + confusion matrix
python3 tools/diag_model.py --model stations            # НА БОРТУ: почему детекций ноль
```

`--manual` (взлёт без пакета) — **только отладка**: на зачётной попытке 3 балла из 5 дают именно за старт по команде от передатчика.

Заглушка умеет ломаться по команде — этим проверяются аварийные ветки автомата: `PIONEER_MOCK_BATT`, `PIONEER_MOCK_DRAIN`, `PIONEER_MOCK_STUCK`, `PIONEER_MOCK_NAV_FAIL`, `PIONEER_MOCK_VIDEO` (`RUN.md` §5). Расстановка станций для проверки перцепции — `PIONEER_MOCK_STATIONS="1.2,0.8,ok; 1.2,-0.8,dust"` и `PIONEER_MOCK_STATION_SIZE=0.35x0.5`.

В референсном дереве (пути относительно `water_object_monitoring_archipelago_2026-develop/`):

```powershell
# Оффлайн-прогон логики по видео: аннотированное видео + detections.json + кропы
python code/run_on_video.py <input.mp4> --out run_output --stride 2

# Тест логики уникального подсчёта (синтетика, без железа)
python code/tests/test_counting.py     # или: pytest code/tests/test_counting.py

# Обучение и сборка датасета
python training/build_dataset.py
python training/train.py
```

Конвертация в `.rknn` и заливка на борт — только с Linux/macOS (Docker amd64, `scp`/`rsync`/`ssh`):

```bash
cd rknn_convert && docker build --platform linux/amd64 -t rknn-conv . \
  && docker run --rm --platform linux/amd64 -v "$PWD":/work rknn-conv
cd stable_v1.2.1 && ./push_to_drone.sh pioneermini@172.17.49.2   # их скрипт по умолчанию берёт 10.42.0.1
```

## Работа с бортом

Точка доступа `PMINI2-<UUID>` / пароль `geoscan123` / **IP нашего борта `172.17.49.2`**
(документация Geoscan и чужой репозиторий называют `10.42.0.1` — у нашего экземпляра адрес
другой, проверено 31.07.2026; при подключении к другому борту смотреть шлюз Wi-Fi);
SSH-логин `pioneermini`. Сервисы (проверены сканированием 31.07.2026): `:22` SSH, `:9090` Pioneer Code, `:9999` Code OSS, `:7777` реестр моделей, `:8889` видеопоток. Рабочая директория на борту — `/home/pioneermini/workspace`.

- **Никаких venv** — в виртуальном окружении `pioneer_sdk2` не работает, ставить только системно.
- После каждой заливки **сносить `__pycache__` на борту**, иначе Python возьмёт старый `.pyc`. `launch.py` делает это сам; заливка живёт только в нём (`план_заливки`) — второго списка файлов в репозитории быть не должно.
- Забытый в другом окне `main.py` держит камеру и выглядит как «дрон сломался»; `launch.py` гасит старые процессы перед запуском.
- Хаб можно поднять **на самом борту** (`--hub drone`, режим по умолчанию у пульта): если на выданной машине нет прав администратора, брандмауэр Windows не пропустит входящее на 5001 и команда на взлёт не дойдёт (`RUN.md` §8.4).
- На **нашем** борту `requests` и `PyYAML` оказались, `flask` — нет (проверено `check_sdk.py` 31.07.2026, вопреки документации). Зависимости от них всё равно не вводим: борт может смениться, а установка требует интернета → `sudo pionet` переводит дрон в режим клиента, после чего **он больше не на `172.17.49.2`**. Ставить в день 1 и возвращать режим точки доступа.
- Камера отдаётся через shared memory: держать её одним процессом и звать `camera.stop()` в `finally`.

## Ограничения платформы заочного этапа

Python **3.8.10** (никакого `list[int]`, `dict | dict`, `match`), OpenCV 4.6, numpy 1.23, torch 1.13, **лимит 5 МБ** на решение. После успешной проверки — пауза 10 минут, после ошибки — сразу; в зачёт идёт лучшая попытка. Отлаживаться локально, платформу как отладчик не использовать.
