# Зависимости по назначению. Устанавливать целиком не обязательно и обычно не нужно.
#
# ГЛАВНОЕ: бортовому ПО дрона из этого файла не нужно НИЧЕГО, кроме того, что уже
# стоит в Pioneer OS (opencv, numpy, pioneer_sdk2, pioneer_rknn). Транспорт написан
# на стандартной библиотеке специально: установка пакетов на борт требует перевести
# дрон в режим клиента Wi-Fi, после чего он перестаёт быть на 172.17.49.2
# (docs/organizer_handouts.md §2.3).

# --- перцепция и запись попыток (на борту предустановлены) ---
numpy
opencv-python

# --- тесты, только на машине разработки ---
pytest

# --- необязательное ---
# flask       хаб поднимется и без него, на http.server из стандартной библиотеки
# PyYAML      config.py читает нужное подмножество YAML сам
