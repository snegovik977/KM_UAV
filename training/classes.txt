# Классы модели станций — ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ для всего конвейера обучения.
#
# Порядок строк = номера классов в YOLO-разметке (первая строка — класс 0) и порядок
# каналов классовой ветки на борту. Он ОБЯЗАН совпадать с detector.labels в
# квадрокоптер/config.yaml: наш декодер разбирает сырые выходы RKNN по числу каналов
# (perception/detector.py -> decode_rawbranch), и лишний или недостающий класс там —
# это не ошибка, а молча неверные боксы. build_dataset.py сверяет эти два списка сам.
#
# Строки, начинающиеся с #, и пустые игнорируются.
#
# ⚠️ КЛАССОВ ДОЛЖНО БЫТЬ НЕ МЕНЬШЕ ДВУХ. При одном классе классовая ветка модели
# имеет 1 канал — ровно как служебная ветка score-sum, и опознать их по числу каналов
# нельзя (docs/YOLO_TRAINING.md §6). Если нужен «просто станция без статуса», всё равно
# держим два класса: ok и dust, а статус на этом этапе можно игнорировать.
#
# ⚠️ station_broken пока НЕ заведён: как выглядит «неисправная» станция, организаторы
# не показали (docs/DRONE_PLAN.md, приложение Г), а класс без единого примера сеть
# выучить не может. Как появятся снимки — дописать сюда третьей строкой `broken`,
# добавить `broken` в detector.labels и переобучить: остальной конвейер не меняется.
ok
dust
