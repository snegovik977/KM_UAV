# Клипы, которые НЕ идут в обучение (по строке на клип, # — комментарий).
# Кадры остаются на месте: по ним удобно сверять правила разметки глазами
# (python training/label.py --clip organizers_phone --review).
#
# ⚠️ В git лежит только РАЗМЕТКА (.txt), сами кадры игнорируются как тяжёлые.
# Восстановить кадры эталонного клипа (имена детерминированы, .txt лягут на место):
#     python training/extract_frames.py "фото станции" --clip organizers_phone --diff 0

# Девять снимков организаторов из `фото станции/`, снятые ТЕЛЕФОНОМ.
# Размечены вручную как эталон формата: пять кадров dust (цветные многогранники
# на панели), четыре ok, мусор и предметы рядом с панелью не размечены.
# В обучение не идут: другая оптика и другой масштаб, чем у камеры борта —
# модель, обученная на них, на дроне поведёт себя непредсказуемо
# (docs/YOLO_TRAINING.md §2).
organizers_phone
